# Base URL of the backend's /api/v1 (see backend/.env.example)
VITE_API_BASE_URL=http://localhost:8000/api/v1

# Must match the backend's API_KEY (see backend/.env.example)
VITE_API_KEY=changeme-dev-key
