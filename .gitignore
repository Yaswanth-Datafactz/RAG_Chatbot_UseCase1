# Python
backend/.venv/
corpus/.venv/
__pycache__/
*.pyc
.pytest_cache/

# Node
frontend/node_modules/
frontend/dist/

# Secrets
.env
backend/.env
frontend/.env
*.env.local

# OS
.DS_Store

# Docker
pg_data/

# Internal planning docs / internship source materials (not for the public repo)
docs/
1_Program_Handbook.docx
2_Week 1_Use_Case_1_RAG_Chatbot_Brief.docx
~$*.docx

# Generated eval output (reproducible by re-running backend/scripts/run_retrieval_eval.py
# against the live index; changes on every run, not hand-authored source)
backend/eval/results.json

# Claude Code local/personal tool settings (machine-specific permission allowlist,
# not shared team config -- see .claude/settings.json convention)
.claude/settings.local.json
